# Copy this file to `.env` and fill in the values before starting the server.
# Values shown are examples — don't commit real secrets to source control.

# Google OAuth (required for Sign in with Google to work)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT / Session secrets (use a long random string in production)
SESSION_SECRET=super-secret-session-key
JWT_SECRET=super-secret-jwt-key

# App URLs (used for OAuth callback redirects)
FRONTEND_BASE_URL=http://localhost:3000
APP_BASE_URL=http://localhost:3000

# OpenRouter AI (optional — if empty, AI features will be disabled)
OPENROUTER_API_KEY=
# Optional OpenAI API key — if you prefer OpenAI instead of OpenRouter you can set this.
# Do NOT wrap the key in extra quotes in the .env file; just paste the raw key value.
OPENAI_API_KEY=

# MySQL (if you want to override the hardcoded values in server.js)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_db_password
DB_DATABASE=resume_architect

# NOTE:
# - After creating .env from this example, restart the server (npm start) so the new variables are loaded.
# - Keep secrets out of version control. Use a secrets manager for production.
